#include<stdio.h>
int main()
{ int t,r,s;
  scanf("%d",&r);
  scanf("%d",&s);
 t=s/2;
 if (r<=t)
   printf("circle can be inside a square");
 else 
   printf("circle cannot be inside a Square");
  //Fill your code
}